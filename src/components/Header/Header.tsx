import NavBar from "../NavBar/NavBar";
import "./styles.scss";

const Header = () => {
  return (
    <div className={"header"}>
      <div className={"container"}>
        <NavBar />
      </div>
    </div>
  );
};

export default Header;
