export type AsideBarFilter = {
  filter: string;
  isSelected: boolean;
  name: string;
};

export type TCities = {
  url: string;
  isSelected: boolean;
  name: string;
};

export type TSortingFilter = {
  filter: string;
  label: string;
  isSelected: boolean;
  isDisabled: boolean;
};
